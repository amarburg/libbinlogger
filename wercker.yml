# wercker version for box creation
box: amarburg/lsdslam-dev-host:conan-latest

build:
  steps:
    - script:
      name: Build
      code: |
          ./fips set config linux-make-unittest
          ./fips gen
          echo "~~~~"
          ls -l /pipeline/libactive_object
          echo "~~~~"
          VERBOSE=1 ./fips build
          ./fips run loggerTest
